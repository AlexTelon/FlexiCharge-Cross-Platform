/// A `PasswordPair` is a class that has two properties, `password1`
/// and `password2`
/// This class is used for the testing of passwords
class PasswordPair {
  String password1;
  String password2;

  PasswordPair(this.password1, this.password2);
}
